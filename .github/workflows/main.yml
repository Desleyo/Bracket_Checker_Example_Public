name: Demo Job
run-name: this action has been run

on: [push]

jobs:
  test: 
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v5
    - uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '10.0.x'
    - run: dotnet build
    - name: test
      run: dotnet test
